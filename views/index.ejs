<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="<%= weatherData ? weatherData.weatherCategory : '' %>">
    <div class="container">
        <header>
            <h1><i class="fas fa-cloud-sun"></i> Weather App</h1>
            <div class="search">
                <form id="weather-form" action="/" method="POST">
                    <input type="text" name="cityName" placeholder="Enter city name" required>
                    <select name="unit" id="unit-select">
                        <option value="metric" <%= weatherData && weatherData.unit === 'C' ? 'selected' : '' %>>°C</option>
                        <option value="imperial" <%= weatherData && weatherData.unit === 'F' ? 'selected' : '' %>>°F</option>
                    </select>
                    <button type="submit"><i class="fas fa-search"></i> Search</button>
                </form>
            </div>
        </header>

        <section class="weather-info">
            <% if (weatherData) { %>
                <div class="current-weather">
                    <h2><%= weatherData.city %>, <%= weatherData.country %></h2>
                    <div class="weather-details">
                        <i class="fas">
                            <img src="http://openweathermap.org/img/wn/<%= weatherData.icon %>@2x.png" alt="weather-icon">
                        </i>
                        <div class="temp-and-condition">
                            <p id="temperature"><%= weatherData.temp %>°<%= weatherData.unit %></p> <!-- Show unit -->
                            <p id="weather-condition"><%= weatherData.weatherCondition %></p>
                        </div>
                    </div>
                </div>
            <% } else if (error) { %>
                <div class="error-message">
                    <p><%= error %></p>
                </div>
            <% } %>
        </section>
    </div>
</body>
</html>
